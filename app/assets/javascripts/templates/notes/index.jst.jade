.shapeshift(layout="row" layout-wrap layout-margin)
  md-card.note-card(ng-repeat="note in notes"
                    flex="20"
                    ng-include="templateForState(note)"
                    ng-mouseenter="note.hover=true"
                    ng-mouseleave="note.hover=false")
